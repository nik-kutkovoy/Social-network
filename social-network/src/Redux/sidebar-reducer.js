let initialState = {
  names: [
    { id: 1, name: "Dima" },
    { id: 2, name: "Egor" },
    { id: 3, name: "Vadim" },
  ],
};

const sidebarReducer = (state = initialState, action) => {
  return state;
};

export default sidebarReducer;
